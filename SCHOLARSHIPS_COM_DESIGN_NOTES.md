# 스콜러십닷컴 "매칭 결과" 페이지 벤치마킹 (호성 전달용)

캡처한 scholarships.com "Scholarship Matches" 페이지 스크린샷을 보고, 지금 우리 결과 화면(`frontend/src/app/spec/page.tsx`, 스펙 입력 후 나오는 카드 리스트)이랑 비교해서 어떤 디자인 요소를 가져올지 정리한 문서. `supabase/matching_gaps.md`가 "매칭 조건(자격조건 필터링)" 갭을 추적하는 문서라면, 이건 그거랑 별개로 **화면/UX** 쪽 결정사항임.

**디자인은 내가 정하고, 호성은 이걸 구현하는 역할.** 아래 1~7번은 채택 확정, 분류 필터는 추가 확정.

---

## 1. 스콜러십닷컴 화면 구성 (스크린샷 기준)

- 상단: 매칭 건수/총 금액 요약 배너 (예: "40 Matched Scholarships", "$444K Matched Scholarship Amount")
- 좌측 사이드바
  - **정렬(Sort By)**: 마감일 / 금액 / 이름 중 택1
  - **보기 필터(Show)**: Matches / Saved(찜) / Applied(지원함) / Won(수혜) / Hidden(숨김) — 탭마다 건수 표시
- 카드 리스트 (한 줄에 하나씩)
  - 제공기관 로고, 이름, 한 줄 설명(말줄임)
  - 금액 / 마감일 아이콘+텍스트
  - 액션 아이콘 4개: 하트(찜) / 체크(지원함 표시) / 트로피(수혜 표시) / 눈감기(숨김)
  - 주황색 "Apply Now" 버튼
  - 광고성 항목은 "Sponsored" 표시

## 2. 지금 우리 화면과 차이점

| 항목 | 스콜러십닷컴 | 유니스코 지금 |
|---|---|---|
| 요약 통계 | 매칭 건수 + 총 매칭 금액 배너 | 건수만 텍스트로 "N건" |
| 정렬 | 마감일/금액/이름 택1 | 없음 (그냥 받은 순서) |
| 상태 관리 | 찜/지원함/수혜/숨김 탭+아이콘 | 없음 |
| 지원 링크 | Apply Now 버튼 | 없음 (링크 자체는 안 보여줌) |
| 마감일 표시 | 카드에 항상 표시 | 카드에 아예 없음 |
| 분류 필터(사이드바) | 없음 (Show 탭은 분류가 아니라 찜/지원 상태) | 없음 — 분류는 카드 위 태그로만 표시 |
| "왜 매칭됐는지" 설명 | 없음 | **있음** — 카드 하단에 지역/나이/소득분위/학점/병역/성별/장애 조건 한 줄 요약 |

마지막 줄은 우리가 이미 스콜러십닷컴보다 잘하고 있는 부분이라 없앨 필요 없음 — 오히려 강점으로 유지.

## 3. 채택 확정 (1~7번)

1. **정렬** (마감일 / 금액 / 이름)
2. **상단 요약 배너** (매칭 건수 + 총 금액)
3. **상태 탭** (찜 / 지원함 / 수혜 / 숨김) + 카드에 하트·체크·트로피·눈감기 아이콘 — **단, 로그인 서비스 구현 이후에 "찜 목록" 형태로 나중에 추가하는 순서로 제안. 호성이랑 순서 논의 필요(확정 아님).**
4. **Apply Now 버튼** (신청 링크로 바로 연결)
5. **마감일을 카드에 항상 표시**
6. **제공기관 로고 이미지**
7. **Sponsored(광고) 표시**

**8번(Students/Parents/Educators 같은 역할별 상단 메뉴)은 제외** — 우리는 학생 단일 대상이라 불필요.

## 4. 좌측 분류 필터 (신규 결정사항)

스콜러십닷컴의 "Show" 탭 자리에, 예전에 정한 우리 분류 체계를 필터로 넣기로 함. 이 분류는 이미 스키마에 있음(`backend/app/models/enums.py`의 `CategoryL1`/`CategoryL2`, 지금은 카드 위 태그로만 쓰이고 필터로는 안 쓰임):

- **전체**
- **교내장학금** (`school_internal`)
  - 성적장학금 / 복지생활지원장학금 / 특수대상장학금 / 활동공로장학금 / 연구장학금 / 국제교류장학금 / 학과동문회자체장학금
- **교외장학금** (`school_external`)
  - 국가장학금 / 지자체장학금 / 민간재단기업장학금 / 협회학회장학금
- **지원금** (`support_fund`)
  - 청년생활지원금 / 활동참여지원금

**동작 방식 (드릴다운, 확정)**:
- 기본값은 "전체" 선택 상태 (지금처럼 다 보임)
- 대분류(교내장학금/교외장학금/지원금) 클릭 → 그 대분류만 필터링되고, 동시에 그 밑에 딸린 중분류 옵션들이 추가로 나타남
- 중분류 클릭 → 거기서 한 번 더 좁혀짐
- "전체"를 다시 누르면 초기화

## 5. 레이아웃 폭 (보류)

지금 결과 화면 전체가 `max-w-md`(약 448px) 폭의 좁은 모바일 스타일 컨테이너 안에 들어있음(`frontend/src/app/spec/page.tsx` 라인 374). 좁아서 가독성이 떨어지는 건 맞지만, 우리는 **모바일 우선(mobile-first)**으로 가는 게 방침이라 지금 단계에서는 폭 조절 안 함. 나중에 노트북/데스크탑용 화면을 따로 만들 때 그쪽 레이아웃(사이드바+넓은 리스트 2단 구성 등)을 다시 논의하기로.

## 6. 호성한테 전달할 구현 관련 확인사항

- **마감일**: `application_period`가 지금 자유텍스트라 정렬(4번 항목)이 안 됨(`matching_gaps.md` #7과 동일 이슈) — 구조화된 date 컬럼으로 바꾸는 작업이 선행돼야 함.
- **찜/지원함/수혜/숨김 상태 저장(3번 항목)**: 사용자별로 저장해야 하는 기능이라 로그인/계정이 있어야 제대로 됨. 순서 제안 — 로그인 서비스부터 구현하고, 그다음에 "찜 목록" 기능으로 붙이는 방향. 이 순서가 맞는지는 호성이랑 논의 필요(아직 확정 아님).
- **`application_url` 값 채움 비율**: 컬럼은 있고 크롤링 때 채우는 중인데(`supabase/README.md`), 133건 중 실제로 몇 건에 값이 들어있는지 확인 필요 — 빈 값인 항목은 Apply Now 버튼을 어떻게 처리할지 정해야 함.
- **분류 필터**: `CategoryL1`/`CategoryL2`는 이미 있고 `CATEGORY_L2_BY_L1` 매핑도 코드에 있어서(`enums.py`) 프론트 필터 구현 자체는 스키마 변경 없이 가능해 보임 — 다만 133건 중 이 필드가 안 채워진 항목이 있는지(백필 여부)는 `SCHEMA_DECISIONS.md` 2026-07-30 항목 참고해서 호성이 확인.

## 7. 장학금 상세 페이지 (신규 결정사항)

스콜러십닷컴처럼 카드/이름을 누르면 우리 사이트 안에서 상세 페이지가 열리고, 그 안의 "신청하러 가기" 버튼을 누르면 외부 지원 링크(`application_url`)로 나가는 구조로 확정. (목록 카드에 있는 Apply Now는 그대로 유지 — 목록에서 바로 나가는 빠른 길, 카드/이름 클릭은 상세 페이지로 들어가는 길, 둘 다 있는 구조.)

리서치 때 확인된 사실: `README.md` 8번 항목에 "장학금 상세 페이지 — 아직 없음. 지금은 목록 카드에 요약 정보만 표시"라고 이미 기존 백로그에 있던 항목 — 이번에 우선순위로 픽업.

**상세 페이지에 들어갈 항목 (초안, 확인 필요)**:
- 이름 / 제공기관 / 분류(대분류·중분류) / 금액 / 마감일 — 목록 카드에도 있는 정보 그대로
- 설명 전체 (목록에서는 말줄임되지만 상세에서는 전문 표시)
- 자격조건 요약 전체 (`eligibilitySummary`에 있는 지역/나이/소득분위/학점/병역/성별/장애 조건)
- **매칭 로직엔 안 쓰이지만 스키마엔 있는 필드들도 상세 페이지에서 보여주면 좋을 듯**(`matching_gaps.md`에 "매칭 안 됨"으로 기록된 필드들이라 지금은 그냥 DB에 잠자고 있는데, 상세페이지 텍스트 표시용으로는 바로 쓸 수 있음): 전공 조건(`major`), 이수학점 조건(`min_credits`), 입학성적 조건(`admission_score_condition`), 선발 인원(`headcount`)
- "신청하러 가기" 버튼 (`application_url`)

이 항목 리스트는 초안이라 — 실제로 이대로 다 보여줄지, 뺄 게 있는지는 다음에 확인.

## 8. 참고

- 현재 결과 화면: `frontend/src/app/spec/page.tsx` (특히 `ScholarshipCard` 컴포넌트, 라인 278-295 근처)
- 매칭 로직: `backend/app/api/match.py`
- 스키마: `backend/app/models/scholarship.py`, `backend/app/models/enums.py`
- 관련 기존 문서: [supabase/matching_gaps.md](supabase/matching_gaps.md), [SCHEMA_DECISIONS.md](SCHEMA_DECISIONS.md), [PROJECT_BRIEF.md](PROJECT_BRIEF.md), [README.md](README.md) 8번 항목("장학금 상세 페이지")
