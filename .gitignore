# Python (backend, harness)
backend/venv/
backend/__pycache__/
backend/**/__pycache__/
harness/venv/
harness/**/__pycache__/
*.pyc

# env files (keep .env.example)
.env
.env.*
!.env.example

# OS
.DS_Store

# Vercel CLI project link (local metadata, not project source — root-level because
# the "unisco" Vercel project's Root Directory setting means `vercel` commands must
# run from the repo root, not frontend/)
.vercel

# personal local notes (not shared)
CLAUDE.local.md
TASKS.local.md

# personal Claude Code settings (not shared)
.claude/settings.local.json

# personal custom slash commands (not shared)
.claude/commands/
