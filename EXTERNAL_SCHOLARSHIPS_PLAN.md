# 외부(재단/기업) 장학금 수집 계획

호성 전달용 — 지금까지는 대학 게시판을 크롤링하다가 재단/기업 장학금을 "우연히" 주웠는데
(용운장학재단, GS SHOP 콘텐츠 리더 장학금 등), 이제부터는 의도적으로 찾으러 가기로 함
(2026-08-05 결정). 이 문서는 그 진행 방식과, 그 과정에서 나온 스키마 관련 결정(부모
거주지역 필드)까지 정리한 기록입니다.

관련 문서: [supabase/matching_gaps.md](supabase/matching_gaps.md) 19번, [SCHEMA_DECISIONS.md](SCHEMA_DECISIONS.md) 2026-08-05 항목

## 범위

지역 제한 없이 전국 대학생(학부/대학원 포함)이 지원 가능한 재단/기업 장학금 + 대전/충남/세종
지역 한정 재단/지자체 장학금. `PROJECT_BRIEF.md`의 "Explicitly out of scope: Nationwide
coverage"와 상충하지 않는지 검토함 — 결론: 이건 "전국 커버리지를 확장하겠다"는 게 아니라
"이미 우리 사용자(대전 대학생)가 지원 가능한데 놓치고 있던 장학금을 채우는 것"이라 스코프
안에 있는 것으로 판단.

## 소스 (어디서 후보를 찾을지)

1. **공공데이터포털 "한국장학재단_학자금지원정보(대학생)"** (data.go.kr, `fileDataId=15028252`)
   — 무료, 상업적 이용 가능("이용허락범위 제한 없음"). 전국 지자체·민간재단·대학 학자금
   정보 1,850건, CSV/API 제공. **중요**: 이 데이터 자체를 그대로 베끼지 않음 — "이런 재단이
   존재한다"는 이름 색인으로만 쓰고, 실제 자격조건·마감일은 반드시 각 재단 원문 사이트에서
   재확인 후 등록(대부분 스냅샷이 오래돼서 마감일이 지나있음 — 587건 중 실제 유효 마감 20건
   뿐이었음, 그래도 매년 반복되는 사업이 대부분이라 이름 자체는 유효한 후보).
   - 이 CSV에서 이미 "운영기관구분=기타(민간/기업)"이면서 지역조건 없는 것 567건, 대전/충남/
     세종 관련 지자체 계열 32건(이 중 **대전광역시서구인재육성장학재단**은 우리 시스템에
     아예 없던 신규 발견 기관)을 1차로 걸러냄 — 사용자 승인, 다음 배치부터 이걸로 진행.
2. **대학 "교외장학금/외부장학금" 게시판** — 대학이 자기 학생용으로 이미 걸러둔 재단
   공고 게시판을 의도적으로 정기 훑기(기존엔 학과 게시판 크롤링 중 우연히 지나가며 주움).
3. **대기업/유명 재단 홈페이지 직접 서치** (보조) — 관정이종환교육재단, 아산사회복지재단,
   현대차정몽구재단, LG연암문화재단, 두산연강재단, 포스코청암재단, SK행복나눔재단,
   삼성꿈장학재단(대학생 대상 여부 확인 필요), 유일한재단, CJ나눔재단, 하나금융나눔재단,
   GS칼텍스재단 등.
4. **드림스폰(dreamspon.com), 캠퍼스콜라(campuscholar.com)** (2026-08-05 추가 — 참고용에서
   실제 소스로 승격) — 둘 다 민간 장학금 정보 모음 사이트로, 공공데이터포털 CSV에 안 걸린
   재단이 여기 있을 수 있어서 이름 목록을 뽑는 용도로 같이 확인. **다른 소스랑 똑같은 원칙
   적용**: 이 사이트들이 정리해둔 내용(자격조건·마감일 등)을 그대로 베끼지 않고, "이런
   장학금이 있다"는 이름만 가져와서 반드시 그 재단/기업 원문 사이트에서 재확인 후 등록.
   캠퍼스콜라는 사실상 유사 서비스라 경쟁 현황 참고 의미도 있음.

## 검증 원칙 (기존 크롤링과 동일, 새로 만든 것 없음)

- 원문 페이지에서 확인 가능한 정보(선발인원/학년제한/거주조건/성적기준/소득기준/마감일)는
  빈칸 없이 전부 채움.
- 사이트가 크롤링을 막아도 우회 가능하면 우회(User-Agent 지정 재요청, PDF/HWP 텍스트 추출,
  이미지 공지 직접 읽기).
- `supabase/scholarship_dedup_list.md`와 이름+기관 대조해서 중복 방지.
- 재단/장학금 자체를 못 찾은 경우 조용히 넘어가지 않고 "확보 실패" 목록에 사유·시도한 방법과
  함께 남겨서 배치 보고에 포함.

## 진행 방식

- 5개씩 배치로 진행, 배치마다 **확인·등록 / 대상 아님(사유) / 못 찾음(사유)** 세 갈래로 보고.
- 확인된 건은 특정 대학 소속이 아니므로 새 파일 `supabase/data_national_foundations.sql`에
  모음(기존 `data_cnu.sql` 등과 동일한 스키마·형식).
- `supabase/scholarship_dedup_list.md`에도 배치 단위로 동일한 표 형식(`name`/`provider`)으로
  기록.

## 기존 데이터 재작업 (부모 거주지역 관련)

`SCHEMA_DECISIONS.md`(2026-08-05 항목)에서 결정한 대로 `SavedSpec.parent_region` 필드를
추가하면, 기존 `eligible_region` 데이터는 값 자체를 바꿀 필요가 없음(자격 지역 값은 본인/부모
공통이라 그대로 유효). 다만 아래는 구현 후 확인 필요:

- "본인 또는 부모" 계열 문구가 `description`에 있는데 `eligible_region`이 비어 있는 기존
  항목이 있는지 감사(현재 알려진 것: 재능키움, 화성시 주거비지원, 인천 해외배낭연수 — 전부
  `eligible_region`이 이미 채워져 있어 값 변경은 불필요, 새 필드 추가만으로 자동 해결됨).
  다만 366건 전체를 이 문구로 다시 훑는 전수 감사는 아직 안 함 — 새로 들어오는 외부 장학금
  배치에서 발견되는 대로 확인.
- 이번에 새로 들어오는 외부 장학금 배치(공공데이터포털 소스)도 "본인 또는 부모" 문구가 자주
  나오는 걸 확인함 — 새로 등록할 때부터 이 패턴이면 `eligible_region`엔 지역 값만 넣고
  `description`에 "본인 또는 부모 중 1인" 조건임을 명시.

## 상태

- 부모 거주지역 스키마 변경: **설계만 완료, 구현 대기** (진행 확인 있으면 다음 세션에 착수).
- 외부 장학금 수집: 소스·검증 방식 확정, 실제 배치 작업은 아직 시작 안 함("시작해" 지시
  있을 때 착수).
