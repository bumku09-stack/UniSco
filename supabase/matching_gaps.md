# 매칭 로직 미지원 자격조건 목록 (호성 전달용)

이 파일은 실행되는 코드가 아니라, `scholarship` 테이블에는 크롤링으로 계속 데이터가 쌓이는데 백엔드 매칭 로직(`backend/app/models/user_spec.py`의 `UserSpec`)이 아직 걸러내지 못하는 자격조건을 놓치지 않고 기록해두는 목록입니다.

**규칙: 크롤링하다가 아래 표에 없는 새로운 미지원 조건이 나오면 이 파일에 바로 추가할 것.**

마지막 업데이트: 2026-08-05. 상세 변경이력은 각 갭 번호 섹션 참고(과거 변경이력 전문은 git 커밋 이력에 그대로 남아있음).

---

## 확인된 사실 (backend/app/models/ 코드 기준, 2026-07-30)

`UserSpec`(사용자 입력값): `university`, `college`, `gpa`, `age`, `gender`, `region`, `military_status`, `income_bracket`, `has_disability`, `is_foreigner`, `enrollment_status`, `grade`, `degree_level`.

`scholarship` 테이블에서 실제로 매칭되는 필드(`backend/app/api/match.py`): `min_age`/`max_age`, `required_gender`, `eligible_region`, `required_military_status`, `max_income_bracket`, `min_gpa`, `requires_disability`, `foreigner_eligibility`, `eligible_university`, `eligible_college`, `required_enrollment_status`, `min_grade`/`max_grade`, `required_degree_level`.

기존 133건 중 128건은 이 신규 필드까지 실제 값으로 백필 완료(2026-07-30, `SCHEMA_DECISIONS.md` 참고). 나머지 필드(`grade_level`, `major`, `affiliated_institution`, `min_credits`, `admission_score_condition`, `headcount`, `application_period`, `category_l1`/`category_l2`)는 **컬럼은 있지만 매칭 로직에서는 전혀 안 쓰임**(`category_l1`/`l2`는 애초에 자격조건이 아니라 분류용이라 매칭 대상이 아닌 게 의도된 설계).

## GPA 관련 참고 (혼동 방지용)

`min_gpa`(장학금 쪽 최소학점 조건)는 **이미 컬럼도 있고 데이터도 채워지고 있음** — 크롤링 중 등급→GPA 환산 기준을 대학마다 한 번 확정해서 적용 중(예: 충남대 A0=4.0/4.5만점, KAIST는 4.3만점제 별도 환산표 사용). 이건 갭이 아님.

`UserSpec.gpa`(사용자가 직접 입력하는 값)의 스케일도 ✅ 해결되어 있음 — `backend/app/core/matching.py`의 `normalized_gpa()`가 `UNIVERSITY_GPA_SCALE` 딕셔너리로 대학별 만점 기준을 4.5만점 기준으로 환산한 뒤 비교함(`gpa_matches()`에서 `spec.semester_gpa`/`spec.cumulative_gpa` 둘 다 이 함수를 거침). 대학 추가할 때마다 `UNIVERSITY_GPA_SCALE`도 같이 갱신할 것.

## 매칭 안 되는 자격조건 목록

| # | 조건 | scholarship 테이블 컬럼 | 상태 |
|---|---|---|---|
| 1 | 소속 대학 | `eligible_university` | ✅ **해결** (2026-07-29, `eligible_university`/`eligible_college` + `UserSpec.university`/`college`로 매칭됨) |
| 2 | 전공/학과 | `major` | ✅ **해결** (2026-08-03, `UserSpec.department`/`SavedSpec.department` 추가 + `core/matching.py`의 `major_matches()`로 매칭. 콤마로 여러 학과가 나열된 기존 `major` 값도 지원(하나만 일치해도 통과). 프론트는 대학→단과대→학과 3단 캐스케이딩 드롭다운(`frontend/src/lib/universities.ts`의 `CollegeInfo.departments`), 목록이 없는 단과대는 자유 텍스트 입력으로 자동 대체됨. **실제 학과 목록도 9개 대학 전부 조사 + 사용자 직접 재확인까지 완료(2026-08-03)** — 아래 "2번 후속" 참고 |
| 3 | 학년/과정(신입생·재학생, 학부·대학원) | `required_enrollment_status`/`required_degree_level` | ✅ **해결** (2026-07-29, `EnrollmentStatus`/`DegreeLevel` enum + `min_grade`/`max_grade`로 매칭됨). **단, 새로 발견된 한계 2건은 아래 "3번 후속" 참고** |
| 4 | 사용자 GPA 입력 스케일 검증 | (UserSpec.gpa 자체) | ✅ **해결** — 위 GPA 참고 항목 참조 |
| 5 | 이수학점 | `min_credits` | 🔶 **랭킹만 부분 해결(2026-08-04)** — 자격 필터링(노출 여부)은 여전히 안 됨(과다노출 유지), 대신 `credit_requirement_condition` 태그로 랭킹 순위만 밀림. 운영 DB 24건 태그 완료. 아래 "18번" 참고 |
| 6 | 입학성적(수능/내신) 조건 | `admission_score_condition` | 🔶 **랭킹만 부분 해결(2026-08-04)** — `school_record_condition` 태그로 랭킹 순위만 밀림(수능은 17번 `suneung_score_condition`으로 분리). 운영 DB 7건 태그 완료. 아래 "18번" 참고 |
| 7 | 마감일(구조화된 날짜) | `application_deadline` | 🔶 **구조는 해결, 데이터는 미백필** (2026-08-03) — `Scholarship.application_deadline`(DATE) 컬럼 추가 + `core/matching.py`의 `deadline_matches()`로 마감 지나면 자동 제외됨. **다만 기존 366건은 전부 NULL로 남아있음**(대부분 "매 학기 초 공지"류 상시 프로그램이라 애초에 고정 마감일이 없는 경우가 많음) — 앞으로 새로 크롤링하는 건 확정 마감일이 있으면 이 컬럼도 채울 것, 기존 데이터 백필은 별도 작업 |
| 8 | 선발인원 | `headcount` | 컬럼 있음(텍스트), 매칭/정렬에 활용 안 됨 |
| 9 | 특수상황 신분(다문화가정·아동양육시설 생활자/퇴소자·학생자치단체 임원 등) | `required_special_status` | ✅ **해결** (2026-08-03) — `SpecialStatus` enum(8종) + `Scholarship.required_special_status`/`SavedSpec.special_status`(둘 다 TEXT[] 다중선택)로 매칭. 매칭 로직이 다른 필드들과 다름 — 유저가 특수상황을 아예 선택 안 하면 조건 있는 장학금도 안 거름(`core/matching.py`의 `special_status_matches()`, 아래 "특수상황 매칭 로직" 참고). 배재사랑장학금처럼 장애 조건과 같이 걸린 경우는 OR로 처리 — 아래 "9·10·12번" 참고 |
| 10 | 외국어 시험 점수(TOPIK/IELTS/TOEIC 등) | `language_test_type`/`language_test_min_score` | ✅ **해결** (2026-08-03, `LanguageTestType` enum + 점수 컬럼으로 매칭됨. 시험 종류까지 정확히 일치해야 함 — `core/matching.py`의 `language_test_matches()` 참고) |
| 11 | 학교 자체 비학점(비교과) 프로그램 이수 조건(한밭대 "비교과 유닛" 등) | 없음 | ✅ **필터링 대상 아님으로 확정(2026-08-01, 사용자 결정)**, 🔶 **2026-08-04 랭킹 페널티만 추가** — `extracurricular_program_condition` 태그로 랭킹 순위만 밀리게 함(운영 DB 8건). 아래 "18번" 참고 |
| 12 | 장애인 세부 유형 | `required_disability_type` | ✅ **해결** (2026-08-03, `DisabilityType` enum(7종, Scholarships.com "Physical Disabilities" 카테고리) + `Scholarship.required_disability_type`/`SavedSpec.disability_type`로 매칭됨 — `core/matching.py`의 `disability_type_matches()` 참고) |
| 13 | GPA "직전학기" vs "전체(누적)" 기준 구분 | `min_gpa_basis` | ✅ **해결** (2026-08-02 컬럼 추가, 2026-08-03 "둘 다 동시 충족" 변형까지 `GpaBasis.BOTH`로 커버 — `core/matching.py`의 `gpa_matches()` 참고. 을지대 "차세대의료인장학금"(id 316)에 적용 완료) |
| 14 | 지자체 장학금의 거주지 세부단위(시/군/구) | `eligible_region` (시/도 단위 문자열만이었음) | ✅ **해결 (2026-08-05)** — `SavedSpec`/`UserSpec.district`(본인)·`parent_district`(부모) 추가 + `region_matches()`가 시/도·시/군/구·부모 시/도·부모 시/군/구 4개 후보를 전부 OR로 검사. 기존 랭킹 태그(`sub_region_residence_condition`)는 이제 자격 필터링까지 되므로 용도 소멸 — 새로 태그할 필요 없음. 아래 "14번" 및 "18번" 참고 |
| 15 | 종교기관 소속·직분·진로 지향 조건(목회자/선교사 자녀, 현직 교회 사역, 향후 목회·선교 지망, 군종사관후보생 등) | 없음 | 자격 필터링은 아직 미해결. 🔶 **2026-08-04 랭킹 페널티 추가** — `religious_or_career_intent_condition` 태그로 운영 DB 14건(한국침례신학대 12건, 배재대·한남대 각 1건) 랭킹 순위만 밀림. 아래 "15번" 및 "18번" 참고 |
| 16 | 부모의 특정 직업/소속 조건(교직원 자녀, 동문 자녀, 교사 자녀, 농업인 자녀, 의용소방대원 자녀, 새마을지도자 자녀 등) | 없음 | 자격 필터링은 아직 미해결. 🔶 **2026-08-04 랭킹 페널티 추가** — `parent_occupation_condition` 태그로 운영 DB 16건 랭킹 순위만 밀림. 아래 "16번" 및 "18번" 참고 |
| 17 | 수능성적 기반 신입생 장학금(계열별 최저학력기준 등) | 없음 (`UserSpec`에 수능 성적 필드 자체가 없음) | 자격 필터링은 아직 미해결. 🔶 **2026-08-04 랭킹 페널티 추가** — `suneung_score_condition` 태그로 운영 DB 18건 랭킹 순위만 밀림. 아래 "17번" 및 "18번" 참고 |
| 19 | "본인 또는 부모 중 1인이 OO에 거주" 거주지 OR 조건(지자체 장학금 다수) | `eligible_region` (본인 거주지만 봤음) | ✅ **해결 (2026-08-05)** — `SavedSpec`/`UserSpec.parent_region`(선택 입력) 추가 + `core/matching.py`의 `region_matches()`로 본인 거주지 OR 부모 거주지 매칭. 아래 "19번" 참고 |
| 20 | 의사상자(義死傷者, 보건복지부 소관) 유족·가족 조건 | 없음 | 자격 필터링은 미해결. 🔶 **2026-08-06 랭킹 페널티 추가** — `righteous_person_family_condition` 태그 신설(코드 반영 완료, DB 반영은 3차 배치와 함께 진행). 아래 "20번" 참고 |

### 9·10·12번(어학점수/장애인유형/특수상황) + 9번 OR복합조건 — ✅ 2026-08-03 전체 구현 완료

세 항목 모두 `SavedSpec`/`Scholarship`에 컬럼 추가 완료, `core/matching.py`의 `language_test_matches()`/`disability_type_matches()`/`special_status_matches()`로 매칭됨(특수상황 로직 상세는 바로 아래 "특수상황 매칭 로직" 참고). 장애인 세부유형은 Scholarships.com "Physical Disabilities" 7종 채택(`physical_impairment`/`learning_disability`/`medical_disability`/`mental_impairment`/`muscular_dystrophy`/`developmental_impairment`/`disabled_parent` — 마지막 값은 본인이 아니라 부모 장애 케이스, 원본 카테고리 그대로 채택). 특수상황은 다중선택(`north_korean_defector`/`multicultural_family`/`child_care_facility`/`student_council_officer`/`single_parent_family`/`grandparent_family`/`multi_child_family`/`national_merit`).

**OR로 묶인 복합조건 장학금**(배재사랑·희망복지·장학사정관·봉사공로, 2026-08-03): `required_special_status`를 단일값→리스트로 바꿔서 4건 재분류. 장애+특수상황이 함께 걸린 배재사랑장학금은 `is_eligible()`에서 둘을 OR로 처리(leniency 예외 없는 `special_status_matches_strict()` 사용). 장학사정관·봉사공로는 일부 세부조건(부모 장애, 활동이력처럼 담을 필드 자체가 없는 것)이 구조화 안 되고 `description` 텍스트로만 남음.

**미해결로 남은 갭(부모 장애 UI 문제)**: `disabled_parent` 값이 프론트 장애인유형 드롭다운에 있는데, 이 드롭다운 자체가 "본인 장애" 토글을 켜야만 열리는 구조라 본인은 장애 없고 부모만 장애인 학생은 이 값을 선택할 방법이 없음 — UI 리디자인 필요(호성·사용자 논의 필요).

### 특수상황 매칭 로직 (2026-08-02, 사용자 확정 — 다른 필드들과 다른 예외 규칙)

다른 필드(성별·나이·GPA 등)는 전부 "장학금에 조건이 있으면, 유저가 그 조건에 안 맞을 때 제외" 방식임. **특수상황은 이거랑 다르게, "유저가 특수상황을 아예 선택 안 했으면 애초에 걸러내지 않는다"는 예외 규칙**을 사용자가 명시적으로 요구함:

- **유저가 특수상황을 하나도 선택 안 함** → 특수상황 조건이 걸려있는 장학금도 걸러내지 않고 그냥 다 보여줌 (마치 특수상황 필드 자체가 없는 것처럼 동작).
- **유저가 특수상황을 1개 이상 선택함** → 그때부터 특수상황 조건이 걸려있는 장학금만 필터링 시작: 유저가 선택한 항목과 장학금이 요구하는 항목이 일치하면 보여주고, 안 맞으면 제외. **특수상황 조건이 아예 없는 일반 장학금은 이 필터링과 무관하게 계속 다 보임.**

**이유(사용자 설명)**: 특수상황은 선택 항목(옵션)이라 안 누른 학생이 많을 텐데, "안 눌렀다"는 게 "나는 특수상황 대상자가 아니다"를 확정하는 게 아님 — 그냥 아직 대답 안 한 것뿐. 그런데 표준 방식대로 처리하면, 실제로는 다문화가정인 학생이 그 항목을 안 눌렀다는 이유만으로 다문화가정 전용 장학금이 안 보이게 될 수 있음 — 그건 잘못된 결과. 그래서 "선택 안 함 = 모르는 것 = 걸러내지 않음", "선택함 = 그 항목 기준으로만 좁힘"으로 설계함.

실제 구현(`backend/app/core/matching.py`) — `required_special_status`가 리스트라 "장학금 쪽 여러 특수상황 중 하나 + 유저 쪽 여러 특수상황 중 하나"가 겹치는지 보는 형태:
```python
def special_status_matches(scholarship_special_status: list[SpecialStatus], spec_special_status: list[SpecialStatus]) -> bool:
    if not scholarship_special_status:
        return True  # 특수상황 조건 없는 일반 장학금 — 항상 통과
    if not spec_special_status:
        return True  # 유저가 특수상황 아예 안 눌렀음 — 그래도 걸러내지 않음
    return bool(set(scholarship_special_status) & set(spec_special_status))  # 하나라도 겹치면 통과
```
`is_eligible()`에는 이 표준 버전 대신, 장애 조건과 OR로 묶일 때만 "안 누르면 통과" 예외가 없는 `special_status_matches_strict()`를 씀(위 "9·10·12번" 참고) — 안 그러면 `has_disability=False`처럼 이미 확실한 "아니오" 답이 있어도 특수상황 쪽 예외 때문에 OR 전체가 사실상 항상 True가 돼버림.

### 2번 후속: 학과(department) 데이터 — ✅ 2026-08-03 완료

`frontend/src/lib/universities.ts`의 `CollegeInfo.departments`로 대학→단과대→학과 3단 구조 구현, `/spec`·`/mypage` 둘 다 연결 완료, 백엔드 매칭(`major_matches()`)도 동작함. 9개 대학(을지대 제외) 전체 학과 목록을 병렬 조사 + 사용자 직접 재확인까지 마쳐서 확정함(자동 조사와 실제 화면이 다를 때는 사용자가 직접 대학 공식 페이지를 열어 확인한 결과가 최종 기준). 학과가 없는 게 맞는 조직(배재대 주시경교양대학/아펜젤러공유대학, 목원대 스톡스대학, 충남대 법학전문대학원·의학전문대학원 등 교양/융합/단일과정)은 빈 배열 유지가 정상.

### 3번 후속: 백필하면서 새로 발견된 한계 (2026-07-30)

`required_enrollment_status`(`undergrad_enrolled`/`undergrad_leave`/`post_undergrad` 3택1) 설계의 잔여 한계:

1. **"학부생·대학원생 둘 다 대상"을 표현 못 함** — 실제로 7건 있었음(KAIST 인성장학금 등). 지금은 비워둠(제한 없음)으로 처리해 결과적으로는 맞지만, 더 세밀한 조건(예: 휴학생만 제외)은 표현 안 됨.
2. **대학원생의 "휴학" 상태를 표현 못 함** — `undergrad_leave`만 있고 대학원 버전이 없음. "학생 출산·육아지원금"(KAIST)은 학부생 기준으로 근사 처리해서 대학원생 휴학 케이스는 놓칠 수 있음.
3. **법학전문대학원(로스쿨) 계열** — ✅ **부분 해결 (2026-07-31)**: `universities.ts`에 `법학전문대학원`/`의학전문대학원` 추가, 로스쿨 장학금 4건은 `eligible_college`로 좁혀서 처리. `required_degree_level`은 JD/MD-PhD 전용 값이 없어 근사 처리(`eligible_college`로 이미 좁혀지므로 결과적으로 문제없음) — 정식 enum 값 추가는 호성과 논의 필요.

### 13번: GPA "직전학기" vs "전체(누적)" 구분 — ✅ 2026-08-02 구현, 2026-08-03 "둘 다" 변형까지 완료

`scholarship.min_gpa_basis`(`GpaBasis` enum)로 `semester`/`cumulative`/`both`/NULL(미지정, 관대한 기본값) 4가지를 구분함. `UserSpec`/`SavedSpec`도 `semester_gpa`/`cumulative_gpa` 두 칸으로 나눠서 받음. 을지대 "차세대의료인장학금"(id 316, 둘 다 동시 충족)이 `both` 값의 계기가 됨 — `core/matching.py`의 `gpa_matches()` 참고.

### 14번: 지자체 장학금의 거주지 세부단위(시/군/구) 매칭 — ✅ 2026-08-05 해결

`region`은 `regionShortName()`을 거치며 구/군 정보가 사라져 시/도 단위(예: "울산")로만 저장됐음 — 시/군/구 단위로 좁혀진 지자체 장학금(정읍시민장학재단 등)을 그대로 매칭하면 과다매칭 위험이 있어 그동안 `eligible_region`을 비워두는 임시방침(과다노출)으로 버텨왔음.

**해결**: `SavedSpec`/`UserSpec`에 `district`(본인)·`parent_district`(부모) 추가. `region_matches()`가 본인 시/도·본인 시/군/구·부모 시/도·부모 시/군/구 4개 후보를 OR로 검사(부분 문자열 매칭). **구현 중 발견한 버그**: 빈 문자열(세종처럼 하위 구/군이 없는 경우 기본값)을 후보에 그대로 넣으면 파이썬의 `"" in "아무거나"`가 항상 True라서 지역 필터링이 통째로 무력화되는 심각한 버그였음 — 빈 문자열은 후보에서 제외하도록 방어 코드 추가, synthetic 테스트로 회귀 확인함.

366건 전체를 재검색해서 정읍시민장학재단·화성시인재육성재단 소상공인 장학금 2건의 `eligible_region`을 "정읍시"/"화성시"로 채움(다른 지자체 조건은 전부 시/도 전체 단위라 이미 정상 매칭 중이었음). 프론트는 `/spec`·`/mypage` 둘 다 부모 거주지역 UI를 본인과 동일한 시/도+구/군 캐스케이딩 드롭다운으로 구현("본인과 동일"/"다름" 토글). 기존 랭킹 전용 태그 `sub_region_residence_condition`(18번)은 이제 자격 필터링 자체가 되므로 용도 소멸(운영 DB 0건).

### 15번: 종교기관 소속·직분·진로 지향 조건 (2026-08-02 발견, 한국침례신학대학교)

침례교단 재단 신학대학인 한국침례신학대학교를 크롤링하면서 발견: 기존 9번(특수상황)의 어느 카테고리에도 안 들어가는, "종교적 신분·직분·진로 지향"을 자격조건으로 하는 장학금이 다수(45건 중 약 12건):

- **특정 교단 인준 목회자 본인/배우자/자녀** (목회자(자녀)장학금, 침례교사모장학금) — "인준된 목회자"라는 자격 자체가 UserSpec에 없는 신분 범주.
- **해외선교사 자녀** (해외선교사(자녀)장학금, 민유선교수기념장학금) — 소속 조건.
- **교회 사역 중(현직)** (손말숙여사기념장학금, 김성진전도사기념장학금) — 현재 활동 상태 조건.
- **향후 목회자/선교사 지망(진로 방향)** (전복단권사이창식집사 기념장학금) — 아직 되지 않은 "장래 진로 희망"을 자격조건으로 삼는 경우, 한남대 "고시장학금"(특정 시험 준비생)과 유사한 어려움.
- **군종사관후보생** (군종사관후보생장학금) — 일반 병역 상태(`required_military_status`)로는 표현 안 되는 별도 축의 신분.

**영향**: 신학대학/기독교 재단 학교(목원대 신학대학, 한남대 등)에서 이런 조건이 반복될 가능성 있음. 지금은 `description`에 텍스트로만 남기고 매칭 필터링에서는 제외함.

**제안(호성 검토 필요, 스키마 변경 사안)**: 교단마다 인준 기준이 다르고 검증도 어려워서, 세분화 enum보다는 계속 `description` 텍스트로만 남기는 게 현실적일 수 있음.

### 16번: 부모의 특정 직업/소속 조건 (2026-08-03 발견, 전체 데이터 재점검 중)

학생 본인이 아니라 **부모(또는 가족)가 특정 직업·소속을 가졌는지**를 자격조건으로 하는 장학금 — 8개 학교/기관, 최소 17건에서 반복됨:

- **교직원 자녀** — 교직원유가족장학금(KAIST), 교직원자녀장학금(대전대), 교직원복지장학금(목원대·배재대), 을지가족장학금(을지대), 교직원자녀장학금(한국침례신학대)
- **동문(졸업생) 자녀** — 동문자녀장학금(대전대·한남대), 배재가족자녀장학금(배재대)
- **일반 교사(교육자) 자녀** — 교육직계장학금(배재대), 교육자직계자녀장학금(한남대) — 15번의 "교역자 자녀"와 달리 종교와 무관
- **특정 직업군 자녀** — 새마을지도자자녀장학금, 의용소방대자녀장학금, 농업인자녀장학금(한국농어촌희망재단), 환경미화원자녀장학금(한남대), 지역화폐 가맹점(소상공인) 자녀 대상(화성시인재육성재단)

**영향**: 어떤 필드로도 "부모의 직업·소속"은 표현이 안 됨 — 과다노출되거나 description 텍스트로만 남음.

**제안(호성 검토 필요, 스키마 변경 사안)**: 15번과 비슷하게 세분화 enum보다는 "학부모 직업/소속 관련 자격조건 있음" 정도의 넓은 플래그 하나만 추가하는 절충안도 검토해볼 만함.

### 14번 후속: 거주지가 아니라 "출신 학교 소재지" 기준인 경우 (2026-08-03 발견)

한남대 "농어촌학생장학금"은 학생의 **현재 거주지가 아니라 출신 고등학교 소재지**가 농어촌 지역인지를 기준으로 함 — `eligible_region`/`UserSpec.region`은 애초에 "현재 거주지" 개념이라 담을 필드가 없음. 14번과는 "거주지 정밀도"가 아니라 "거주지 vs 출신 학교 소재지"라는 다른 축의 문제라 별도 기록.

### 17번: 수능성적 기반 신입생 장학금 매칭 안 됨 (2026-08-03 발견)

충남대 "CNU Honor Scholarship 1·2", "CNU Community Scholarship", "글로벌엘리트 A" 같은 신입생 전용 장학금은 실제로는 **수능 응시계열(문과/이과)** + 등급 조건인데(원문: "자연계열 신입생 중 수능 반영 영역 모두 3등급 이내"), `UserSpec`에 수능 성적을 입력받는 필드 자체가 없어서 매칭이 불가능함.

- **임시 조치(2026-08-03)**: 해당 4건(충남대 3건 + 한국침례신학대 1건) `major` 값을 NULL로 비워 과다노출 처리. 충남대 3건은 `min_grade=max_grade=1`로 신입생 한정은 되지만, 침례신학대 1건은 학년 제한이 없어 학교 전체에 노출됨.
- **제안(호성 검토 필요, 스키마+프론트 변경 사안)**: `UserSpec`에 수능 성적 입력 필드 추가 검토. 사용자 아이디어: 스펙 1페이지에서 "재학생"+"1학년"을 고르면 이미 "신입생으로 인식돼요"라고 표시되는 기존 로직(`frontend/src/app/spec/page.tsx`, 구현되어 있음)을 활용해 **신입생으로 판정된 사용자한테만** 3페이지에 수능 성적 입력 칸을 보여주는 방식. 구체적인 입력 UI는 아직 미정.

### 18번: 매칭적합도순 재설계 + "확인 불가 조건" 랭킹 페널티 (2026-08-04)

기존 랭킹(`specificity_score()`)이 "학생과 얼마나 잘 맞나"가 아니라 "장학금이 조건을 얼마나 많이 걸었나"로 순위를 매기고 있던 문제(사용자 지적)를 고침.

**랭킹 공식 교체** (`backend/app/core/matching.py`): `confirmed_match_count()`로 교체 — 나이/성별/거주지/병역/GPA/장애/외국인여부/전공/대학/단과대/재학상태/학년/학위과정 13개는 항상 세지만, 소득분위·어학점수·특수상황 3개는 학생이 실제로 값을 입력했을 때만 셈(안 넣었는데도 leniency로 통과된 걸 "확인된 매칭"으로 잘못 세던 버그 수정). 새 정렬 키 `personal_fit_key()` = `(ratio, confirmed)` 내림차순, `ratio = confirmed / (confirmed + unverifiable)`.

**"확인 불가" 태그 8개 추가** (`SpecialStatus` enum, 기존 `required_special_status` 컬럼 재사용, 학생은 절대 선택 불가 — 크롤링 데이터 전용): `parent_occupation_condition`(16번)·`religious_or_career_intent_condition`(15번)·`sub_region_residence_condition`(14번)·`hometown_school_region_condition`(14번후속)·`suneung_score_condition`(17번)·`school_record_condition`(6번)·`credit_requirement_condition`(5번)·`extracurricular_program_condition`(7번/11번). 노출 정책(과다노출)은 안 바뀌고 순위만 밀림 — `is_eligible()`이 이 8개 태그를 먼저 걸러내고 나머지로만 판단해서, 학생이 특수상황을 골라도 이 태그 붙은 장학금이 실수로 숨겨지지 않도록 설계.

**데이터**: 운영 DB 366건 중 82건 태그 완료(태그별 건수: credit 24·suneung 18·parent_occupation 16·religious 14·extracurricular 8·school_record 7·hometown_school_region 1·sub_region 0).

**어학점수 버그**: `language_test_matches()`가 "안 넣음"과 "다른 시험 넣음"을 구분 못 하고 둘 다 탈락시키던 버그도 같이 수정(안 넣으면 이제 정상 통과).

**같은 날 후속 수정**: `unverifiable_condition_count()`가 처음엔 `spec`을 안 받아서, 학생이 특수상황을 안 골랐는데 그 조건 걸린 장학금이 감점 없이 상단에 뜨는 회귀가 있었음(북한이탈주민 장학금이 계속 상위 노출되는 걸 사용자가 발견) — `spec`도 받게 고쳐서 소득분위/특수상황/어학점수 미입력 시에도 8개 확인불가 태그와 동일하게 감점 대상에 포함시킴.

### 19번: "본인 또는 부모 거주지" OR 조건 (2026-08-05 발견 및 해결)

지자체 장학금은 "본인 또는 부모 중 1인이 OO에 거주"처럼 부모 주소지 기준으로도 자격이 되는 OR 조건인 경우가 대부분인데, 기존 로직은 `spec.region`(본인 거주지)만 봐서 부모님만 해당 지역에 사는 학생이 과소매칭되는 문제가 있었음.

**해결**: `SavedSpec`/`UserSpec`에 `parent_region`(선택 입력) 추가, 스콜라십 쪽 스키마는 안 건드리고 `region_matches()`만 "본인 거주지 OR 부모 거주지"로 확장(`eligible_region`이 지역명 하나뿐이라 어느 쪽이 그 지역이든 결과가 같음). `parent_region` 안 넣으면 기존과 동일하게 본인 거주지만 판단 — 랭킹 페널티 대상 아님(부모 거주지는 몰라도 되는 선택 정보일 뿐, 원래도 본인 거주지 기준이었으므로 회귀 아님).

시/도 단위로 지역 조건 걸린 스콜라십 전체(346건)에 일괄 적용(OR로 넓히는 방향이라 기존 매칭이 틀려질 위험 없음). 14번(시/군/구 세부단위)과는 별개 — `eligible_region`이 시/도 단위만 담고 있어 시/군/구로 좁혀진 장학금(화성시 등)은 이번 변경의 영향을 안 받음. 프론트는 `/spec`·`/mypage` 둘 다 기존 "선택 입력" 토글 패턴으로 추가(기본값은 본인 거주지역으로 미리 채움).

상세는 `SCHEMA_DECISIONS.md` 2026-08-05 항목 참고.

### 20번: 의사상자(義死傷者) 유족·가족 조건 (2026-08-06 발견 및 랭킹 태그 추가)

외부 장학금 3차 배치(상품 수 많은 지자체 재단 25곳) 조사 중 **서로 다른 3개 기관(청송군·
공주시·제주도)에서 독립적으로 발견** — "직무 외 구조행위 중 사망·부상해 보건복지부장관이
의사상자로 인정한 사람"의 자녀·유족 대상 조건. `national_merit`(국가유공자)로 잘못 태그하면
안 됨 — 국가유공자는 국가보훈부 소관 별도 법률(독립유공자·참전유공자 등)이고, 의사상자는
보건복지부 소관의 완전히 다른 법률(「의사상자 등 예우 및 지원에 관한 법률」) 대상이라 법적
근거 자체가 다름.

**처리**: 15~17번과 같은 패턴 — `SpecialStatus`에 "확인 불가" 랭킹 태그
`righteous_person_family_condition` 신설(`backend/app/models/enums.py`, 코드 반영 완료).
학생이 선택할 수 없고, 노출(과다노출)은 그대로 유지한 채 순위만 밀림. 3차 배치 데이터에 이
태그 반영 예정.

**참고(같은 배치에서 함께 정리한 것)**:
- "이장·통장·반장(지역 행정보조직) 재직자 본인/자녀" 조건(횡성·춘천·공주시 3곳에서 발견)은
  새 태그를 만들지 않고 기존 **16번(`parent_occupation_condition`, 부모 직업/소속 조건)**으로
  분류함 — 이미 그 카테고리에 새마을지도자·의용소방대원 같은 "지역 공로직" 사례가 있어서
  같은 성격으로 봐도 무리 없음.
- "특정 개별 학교(예: 강원 북평고) 졸업생 한정" 조건, "향우회 등 비거주 출신자(타지 거주자도
  대상)" 조건(영암군·거창군)은 새 태그 대신 기존 **14번 후속(`hometown_school_region_condition`,
  출신 학교/출신지 기준 조건)의 적용 범위를 넓혀서** 흡수함(코드 주석에 반영 완료) — "현재
  거주지가 아니라 출신지 기준"이라는 핵심이 같아서 새 카테고리를 또 만들 필요가 없었음.
- "제주 의용소방대원·지역자율방재단원·청소년지도위원·새마을지도자 자녀" 4건도 16번
  `parent_occupation_condition`으로 이미 커버됨(조사 에이전트가 "새 유형"으로 잘못 보고했던
  것을 재검증 과정에서 정정함).
- "장애/장애인 등록" 조건이 이번 배치에서 8건 이상 반복 발견됐는데, 이건 갭이 아니라
  조사 에이전트 브리핑 실수였음 — `requires_disability`(본인 장애 여부 bool)가 이미 있는
  필드인데 이번 조사 지시에 특수상황 13개 선택지만 나열하고 이 필드를 언급 안 해서 에이전트들이
  "장애 관련 필드가 없다"고 착각·보고함. 실제로는 전부 `requires_disability=true`로 정상
  구조화 가능 — 3차 배치 데이터 반영 시 이렇게 처리함.

### 소득기준 "중위소득 N% 이하" 표기 — 갭은 아니지만 개선 여지 있음 (2026-08-06 발견)

3차 배치 25개 기관 중 상당수(전남·영암군·영광군·제주도 등 특히 지자체 재단)가 소득 조건을
한국장학재단의 "학자금지원구간(소득분위 1~10구간)"이 아니라 **"소득인정액 기준 중위소득
N% 이하"**로 표기함(예: "중위소득 150% 이하"). 지금은 이런 경우 `max_income_bracket`을
비워두고 원문 그대로 `description`에만 남기는 식으로 처리 중 — 잘못된 값을 넣느니 비워두는
게 안전하다는 기존 원칙에 따른 것으로, 데이터 정합성 문제는 아님.

**개선 여지**: 학자금지원구간 자체가 원래 "기준중위소득 대비 소득인정액 %"를 구간별로 나눠
정의한 것이라(예: 1구간=중위소득 30%이하 ... 10구간=200%초과 식), 한국장학재단이 매년
공식 발표하는 구간별 정확한 % 경계값을 확인해서 변환표를 하나 만들어두면, 이런 "중위소득 N%"
표기 장학금들의 `max_income_bracket`도 채울 수 있게 됨(지금은 매 배치마다 그냥 비워두고
넘어가는 중이라, 이 표 하나만 만들면 소급 적용도 가능). **아직 실행 안 함 — 우선순위 정해서
진행할지 판단 필요.**

### eligible_region의 시간(거주기간/전입시점) 정밀도 한계 — 갭은 아니지만 기록 (2026-08-06)

`eligible_region`은 "지금 그 지역에 사는지"만 보는데, 실제로는 정반대 방향의 조건들이 꽤
있음: (1) "최근 3개월~1년 이내에 전입한 사람만"(춘천 주소이전장학금, 음성 전입장학금,
공주 공주사랑장학금 — 오래 산 사람이 아니라 **최근에 이사 온 사람**), (2) "30년 이상
계속 거주"(용인 향토장학금 — 일반적인 1~2년 거주 기준보다 훨씬 김). `eligible_region`
필드는 거주 기간·시점을 전혀 안 보기 때문에 이런 조건은 전부 과다매칭 방향으로 남음
(원래 못 받을 사람한테도 뜸). 발생 빈도가 아직 낮아서(3차 배치에서 3~4건) 당장 스키마
변경까지는 필요 없어 보이지만, 계속 쌓이면 `SavedSpec`에 거주 시작 시점 같은 필드가
필요해질 수 있음 — 기록만 해둠.

---

## 그 외 호성과 논의할 사항 (자격조건 매칭과는 별개)

1. **UserSpec 저장** — 🔶 부분 해결(2026-07-29): 로그인 없이 프론트 `localStorage`에 클라이언트 저장하는 방식으로 일단 재입력 부담은 줄임(`user_spec.py` 주석: "Once auth exists this should move to a real per-user table instead"). 서버 쪽 진짜 계정/프로필 저장은 아직 없음 — 나중에 로그인 도입 여부 결정 필요.
2. **DB에 중복 방지 제약이 없음** — 지금 중복 제거는 `scholarship_dedup_list.md`를 사람이 손으로 대조하는 방식뿐. 대학이 늘어날수록(한밭대·카이스트 이후 계속) 깨지기 쉬움 — `name+provider` 유니크 제약이나 INSERT 전 자동 중복검사를 백엔드에 둘지 논의. **(아직 미해결)**
3. ~~새 필드 추가 시 프론트엔드도 같이 손봐야 함~~ — ✅ 해결(2026-07-29, 대학/단과대/학년/재학상태 드롭다운 있는 스펙 입력 마법사(`frontend/src/app/spec/page.tsx`)로 이미 구현됨).
4. **마감일 자동 정리** (위 표 7번의 연장) — 대학이 늘어나면 이미 넣은 장학금들의 마감이 하나둘 지나갈 텐데, 주기적 재크롤링/정리 계획이 필요한지, 구조화된 deadline 컬럼으로 자동 필터링할지 방향 결정 필요. **(아직 미해결)**
5. ~~한밭대·배재대·목원대·우송대·한남대가 `frontend/src/lib/universities.ts` 대학 드롭다운에 없음~~ — ✅ **해결 확인(2026-08-02)**: 최근 커밋(`e42d0c9`)에서 5개 대학 전부 추가됨(코드 확인 완료).
6. **`data_baejae.sql`·`data_hanbat.sql`의 배치1 행(원래 크롤링 당시 입력분) 중 다수가 컬럼 개수 불일치** (2026-08-02 우연히 발견) — 두 파일 다 컬럼 헤더는 29개(당시 스키마 기준)를 선언하는데, 실제 각 행의 값 개수를 세어보니 배재대 21건 중 19건, 한밭대 20건 중 19건이 28개 값만 가지고 있음(1개씩 부족). **라이브 DB(Supabase)에는 이미 정상적으로 들어가 있어서 서비스에는 영향 없음** — 문제는 이 두 백업 파일을 그대로 재적재(reseed)하면 에러가 날 거라는 것. 재적재를 실제로 쓸 일이 생기기 전까지는 급하지 않아서 보류. **(아직 미해결)**
7. **description(원문 설명)엔 조건이 적혀 있는데 구조화 컬럼은 비어있는 "전사 누락" 실수 — 데이터 신뢰 문제로 발견** (2026-08-07) — 실제 사용자가 "수의학과"를 입력했는데 "문화예술인재학부장학금"(현대차 정몽구 재단, 음악·무용 전공 전용)이 추천되는 걸 발견해서 드러남. 원인: description에 "해당 전공자 외 지원불가"가 다 적혀 있었는데 `major` 컬럼이 NULL이었던 것 — `matching.py`의 거의 모든 매칭 함수가 "구조화 필드 비어있음 = 조건 없음"으로 설계돼 있어서(진짜 애매한 경우를 위한 의도적 설계) 정반대로 작동해 전 국민에게 노출됨. 같은 유형이 금액(`amount`)·인원(`headcount`)·기간(`application_period`)에도 있었음(대전 "꿈돌이장학금 등"은 `amount` NULL인데 다른 컬럼엔 `'확인 필요'`라는 메모 문자열이 실제 값처럼 그대로 들어있었음). **대응**: (1) `supabase/tools/description_gap_check.py` — description 안 키워드/정규식으로 "조건은 있는데 구조화 컬럼 비어있음"과 "플레이스홀더 문자열이 값 자리에 들어감"을 잡아내는 공용 판정 함수. (2) `supabase/tools/audit_description_gaps.py` — 라이브 DB 전체를 이 규칙으로 스캔해서 리포트 생성(읽기 전용). (3) `supabase/tools/batch_validation.py` — 앞으로 새 배치(`gen_batchN.py` 패턴)에 하드 게이트로 삽입, 걸리면 SQL 자체를 생성 안 함. (4) `.claude/commands/audit-scholarships.md` — 기존 데이터 재감사를 사용자가 원할 때 수동으로 트리거하는 커스텀 슬래시 커맨드. **한계**: 키워드/정규식 기반이라 완벽하지 않음(자연어 표현은 무한하므로) — 그래서 662건 전체를 조사 에이전트로 원문(`application_url`)까지 다시 열어 대조하는 심층 재검증도 별도로 진행 중(2026-08-07 착수). 감지기가 놓친 실제 사례가 나올 때마다 `description_gap_check.py`의 규칙에 반영해서 계속 키워나갈 것.

**662건 전수 심층 재검증 결과 (2026-08-07 완료)**: 12개 병렬 조사 에이전트로 662건 전체를 description 재추출 + 원문 대조. 약 150개 장학금에서 문제 발견, 그중 완전히 새로운 유형의 자격조건 8개 발견(전공 "제외" 조건/타 장학금 기수혜 전제/군 위탁교육생·ROTC 신분/복수전공 이수 조건/가족 동시재학 조건/해외대학 재학 전제/국가보훈부 등록 장기복무 제대군인/귀농귀촌 정착민 신분). 처리 현황:
- ✅ **1단계 완료(2026-08-07)** — "확신도 높음 + 기존 컬럼 값만 채우면 되는" 64건을 `supabase/tools/fix_batch_2026-08-07.py`로 일괄 반영(headcount 5건/min_credits 8건/min_grade·max_grade 8건/application_deadline 7건/특수상황 태그 9건/major 4건/degree_level 단일값 한계 완화 4건 등). `/match` API로 샘플 검증 완료(대학원생 학위과정 필터, 전공 필터 둘 다 정상 동작 확인).
- 🔶 **2단계 결정만 되고 아직 미구현** — (a) `excluded_major` 컬럼 신규 추가(전공 "제외" 조건, id=257/1134 대상), (b) `special_status_match_all` 플래그 추가(특수상황 AND 조건, id=1019/1046/1052 대상), (c) id=964(음성군 해외장학생, 국내 대학생 대상 서비스 취지와 안 맞음) DB에서 삭제. **(아직 미해결 — 다음 작업)**
- ⏸ **3단계 보류** — 확신도 "중간/낮음"(원문 링크가 없거나 접근 안 돼서 못 본 것들, 약 60여 건). 12개 청크 원본 리포트는 세션 스크래치패드에 있었음(재사용 시 다시 조사 필요할 수 있음). **(아직 미해결, 여유될 때 재검증)**
- 참고(급하지 않은 나머지 구조적 한계): `required_degree_level` 단일값이라 "석사 또는 박사" 동시 표현 불가(부분적으로 1단계에서 완화 처리), `eligible_region`이 "거주지"와 "출신 학교 소재지"를 혼동하는 사례 다수, `backend/app/core/matching.py`의 `UNIVERSITY_GPA_SCALE`에 서울대학교 누락(코드 이슈, id=1105 발견).

**1단계 반영 직후 실사용자가 직접 발견한 추가 버그 2건 (2026-08-07, 즉시 수정 완료)**:
1. id=666 KT디지털인재 장학생 — `major`가 NULL이라 ICT 무관 전공자에게도 노출되던 것을 수정(컴퓨터공학과/소프트웨어학과/인공지능학과/데이터사이언스학과/정보통신공학과/전자공학과로 채움). 원래 12개 청크 감사에서 이미 찾았던 항목인데 확신도 "중간"이라 1단계 일괄반영에서 제외됐던 것 — 실사용에서 재확인되어 바로 수정.
2. **"구/군 이름 충돌" 로직 버그(코드, `region_matches()`)** — 대전 중구 거주 사용자한테 "인천광역시 중구·미추홀구·연수구" 한정 장학금(id=1048)이 추천되는 걸 발견. 원인: "중구"라는 구 이름이 서울/부산/대구/인천/대전/울산에 전부 있는데, 기존 매칭은 구/군 이름을 시/도 확인 없이 순수 부분 문자열로만 비교해서 발생. `backend/app/core/matching.py`의 `region_matches()`를 수정 — eligible_region 안에 시/도 이름이 박혀 있으면(예: "인천광역시") 구/군 후보가 매칭되더라도 사용자의 시/도가 그 시/도와 일치하는지까지 추가로 확인하도록 변경. eligible_region에 시/도 이름이 없는 기존 컨벤션(구/군 이름만, 예: "정읍시")은 영향 없음 — 정읍시/화성시 등 기존 정상 케이스 회귀 테스트 통과 확인(unit 테스트 8건 + 실제 `/match` API 4건). 발견 경위 자체가 "3겹 안전망" 중 실사용 피드백 층이 실제로 작동한 사례.

---

## 참고

관련 크롤링 규칙: [supabase/scholarship_dedup_list.md](scholarship_dedup_list.md), [supabase/README.md](README.md)
